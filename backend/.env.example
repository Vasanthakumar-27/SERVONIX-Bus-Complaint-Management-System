DB_HOST=127.0.0.1
DB_USER=root
DB_PASSWORD=
DB_NAME=bus_complaints

# =============================================================
# SMTP Email — Gmail (recommended for OTP delivery)
# =============================================================
# Step 1: Enable 2-Step Verification on your Google Account:
#           https://myaccount.google.com/security
# Step 2: Create an App Password:
#           Google Account → Security → App passwords
#           App: Mail | Device: Windows Computer → Generate
# Step 3: Copy the 16-character App Password (no spaces) below
# =============================================================
EMAIL_SENDER=your-gmail@gmail.com
EMAIL_PASSWORD=xxxx xxxx xxxx xxxx
EMAIL_FROM_NAME=SERVONIX
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
# SMTP_TIMEOUT=20   # seconds before giving up (default 20)

# Alternative providers:
#   Outlook: SMTP_SERVER=smtp-mail.outlook.com  SMTP_PORT=587
#   Yahoo:   SMTP_SERVER=smtp.mail.yahoo.com    SMTP_PORT=587

# =============================================================
# Resend API (alternative — overrides SMTP when set)
# Free tier: 3,000 emails/month — https://resend.com
# =============================================================
# RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxx
# RESEND_FROM=SERVONIX <noreply@yourdomain.com>

# =============================================================
# Flask / Security
# =============================================================
SECRET_KEY=change-this-secret-key-in-production
JWT_SECRET=change-this-jwt-secret-in-production
